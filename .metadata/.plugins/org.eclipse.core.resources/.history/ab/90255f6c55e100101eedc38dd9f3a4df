plugins {
    id 'nexus.java-conventions' // 우리가 만든 법전 적용
    // application 플러그인은 'main' 메서드를 실행하기 위해 필요합니다.
    id 'application' 
}

dependencies {
    // 🔥 핵심: 우리가 만든 웹 스타터 하나만 넣으면, Core/Web/Validation이 다 딸려옵니다.
    implementation project(':modules:nexus-web-starter')
}

application {
    // 실행할 메인 클래스 위치 (잠시 후 만들 예정)
    mainClass = 'com.example.demo.NexusSampleApplication'
}